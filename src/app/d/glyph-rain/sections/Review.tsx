import ReviewWorkspace from './ReviewWorkspace';

/**
 * Act IV — source beside translation, revision state carried by type alone.
 * The workspace is fork-local (sections/ReviewWorkspace.tsx, the toolchain's
 * typing workspace rescoped): four real rows, one writing itself at a time.
 */
export default function Review() {
  return <ReviewWorkspace />;
}
